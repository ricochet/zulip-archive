[
    {
        "content": "<p>pchickey opened <a href=\"https://github.com/bytecodealliance/wasmtime/issues/5641\">issue #5641</a>:</p>\n<blockquote>\n<p>Wasmtime provides <code>WasmStr</code> and <code>WasmList&lt;T&gt;</code>, which impl <code>Lift</code>, which make it possible for import function parameters to borrow strings and (primitive) lists from the canonical ABI caller's linear memory, rather than pass the import function an owned copy.</p>\n<p>Presently, import bindings will always have Wasmtime create an owned copy, and pass it directly to the trait method.</p>\n<p>It would be better if import bindings have Wasmtime pass the WasmStr/WasmList version of these args, and transform those to the <code>&amp;str</code>/<code>&amp;[_]</code> forms, and pass those borrowed values to the trait method.</p>\n</blockquote>",
        "id": 323749452,
        "sender_full_name": "Wasmtime GitHub notifications bot",
        "timestamp": 1674747157
    },
    {
        "content": "<p>alexcrichton edited <a href=\"https://github.com/bytecodealliance/wasmtime/issues/5641\">issue #5641</a>:</p>\n<blockquote>\n<p>Wasmtime provides <code>WasmStr</code> and <code>WasmList&lt;T&gt;</code>, which impl <code>Lift</code>, which make it possible for import function parameters to borrow strings and (primitive) lists from the canonical ABI caller's linear memory, rather than pass the import function an owned copy.</p>\n<p>Presently, import bindings will always have Wasmtime create an owned copy, and pass it directly to the trait method.</p>\n<p>It would be better if import bindings have Wasmtime pass the WasmStr/WasmList version of these args, and transform those to the <code>&amp;str</code>/<code>&amp;[_]</code> forms, and pass those borrowed values to the trait method.</p>\n</blockquote>",
        "id": 323749453,
        "sender_full_name": "Wasmtime GitHub notifications bot",
        "timestamp": 1674747157
    }
]